let numarray = [1, 2, 3, 4, 5, 6, 7, 8, 9];
console.log(numarray.includes(8));
//by using includes function we can check wether it is in the erray or not
// it is the shortest way to find the value in the erray or
